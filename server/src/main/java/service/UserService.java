package service;

import server.RegisterRequest;
import server.RegisterResult;

public class UserService {
    public static RegisterResult register(RegisterRequest request) throws TakenException, RequestException {

    }
}
